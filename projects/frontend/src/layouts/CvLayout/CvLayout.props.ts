import { ReactNode } from 'react'

type CvLayoutProps = {
  className?: string
  id?: string
  header: ReactNode
  avatar: ReactNode
  main: ReactNode
  aside: ReactNode
}

export default CvLayoutProps
